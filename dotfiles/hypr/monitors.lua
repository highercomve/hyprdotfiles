-- Monitor setup (migrated from monitors.conf).
-- NOTE: nwg-displays only writes the hyprlang monitors.conf — if you
-- regenerate with it, port the values here manually.

hl.monitor({
    output   = "DP-1",
    mode     = "2560x1440@164.96",
    position = "0x0",
    scale    = 1.0,
})
